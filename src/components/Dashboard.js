import React from 'react';
import '../css/dashboard.css';

 

export const Dashboard = (() => {
    return (
        <div className="container container-content main-section">
            <div className="col" align="center">
                <div className="h5 text">Sign in with your tezos account to view incoming streams</div>
                <button type="button" onClick={()=>{}} className="btn btn-primary">Sign in</button>
            </div>
        </div>
    );
});